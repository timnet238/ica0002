---
  - name: Initial setup
    hosts: all
    roles:
      - init
    become: yes

  - name: Database server
    hosts: db_servers
    roles: 
      - mysql
    become: yes
      
  - name: Web server
    hosts: web_servers
    roles:
      - nginx
      - agama
      - uwsgi
    become: yes
